const Worker = require("../models/worker.model");
const File = require("../models/file.model");

const mongoose = require("mongoose");



module.exports.progressBar = (req, res, next) => {
req.params.id
  Worker.findOne()


}